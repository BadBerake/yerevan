# راهنمای استخراج اطلاعات کافه‌های ایروان از 2GIS

این پروژه شامل دو اسکریپت برای استخراج اطلاعات کافه‌های شهر ایروان از سایت 2GIS است.

## 📋 فهرست

1. [روش اول: استفاده از API رسمی](#روش-اول-api-رسمی)
2. [روش دوم: Browser Automation](#روش-دوم-browser-automation)
3. [نصب Dependencies](#نصب-dependencies)
4. [خروجی](#خروجی)
5. [عیب‌یابی](#عیب‌یابی)

---

## روش اول: API رسمی

### مزایا
- ✅ سریع‌تر
- ✅ داده‌های ساختاریافته
- ✅ قانونی و پایدار
- ✅ دقیق‌تر

### معایب
- ❌ نیاز به ثبت‌نام و API Key
- ❌ محدودیت تعداد درخواست در نسخه رایگان

### مراحل اجرا

#### 1. دریافت API Key

1. به [Platform Manager](https://platform.urbi.ae/) بروید
2. ثبت‌نام کنید یا وارد شوید
3. یک Demo Key رایگان ایجاد کنید
4. API Key را کپی کنید

#### 2. تنظیم اسکریپت

فایل `2gis_api_scraper.py` را باز کنید و در خط 24، API Key خود را جایگزین کنید:

```python
API_KEY = "YOUR_API_KEY"  # API Key خود را اینجا قرار دهید
```

#### 3. نصب Dependencies

```bash
pip install -r requirements.txt
```

#### 4. اجرای اسکریپت

```bash
python 2gis_api_scraper.py
```

---

## روش دوم: Browser Automation

### مزایا
- ✅ نیازی به API Key ندارد
- ✅ بدون محدودیت درخواست
- ✅ می‌تواند اطلاعات بیشتری استخراج کند

### معایب
- ❌ کندتر است
- ❌ نیاز به نصب Chrome Driver
- ❌ ممکن است با تغییرات سایت مشکل داشته باشد

### مراحل اجرا

#### 1. نصب Chrome و ChromeDriver

**macOS:**
```bash
brew install chromedriver
```

**Linux:**
```bash
sudo apt-get install chromium-chromedriver
```

**Windows:**
1. [ChromeDriver](https://chromedriver.chromium.org/) را دانلود کنید
2. فایل را در PATH سیستم قرار دهید

#### 2. نصب Dependencies

```bash
pip install -r requirements.txt
```

#### 3. اجرای اسکریپت

```bash
python 2gis_selenium_scraper.py
```

**نکته:** برای اجرای headless (بدون نمایش مرورگر)، در فایل `2gis_selenium_scraper.py` خط 42 را uncomment کنید:

```python
chrome_options.add_argument("--headless")
```

---

## نصب Dependencies

### نصب همه Dependencies

```bash
pip install -r requirements.txt
```

### نصب فقط برای API Scraper

```bash
pip install requests pandas
```

### نصب فقط برای Selenium Scraper

```bash
pip install selenium requests pandas beautifulsoup4
```

---

## خروجی

هر دو اسکریپت دو فایل تولید می‌کنند:

### 1. فایل JSON
- **API Scraper:** `yerevan_cafes.json`
- **Selenium Scraper:** `yerevan_cafes_selenium.json`

ساختار داده‌ها:
```json
[
  {
    "id": "70000001088928304",
    "name": "Achajour",
    "address": "Yerevan, Mashtots Avenue, 18",
    "phone": "+374 10 523456",
    "website": "https://achajour.am",
    "latitude": "40.186547",
    "longitude": "44.506545",
    "working_hours": "Mon-Sun: 08:00-23:00",
    "rating": "4.5",
    "reviews_count": "127",
    "rubrics": ["Cafe", "Restaurant"]
  }
]
```

### 2. فایل CSV
- **API Scraper:** `yerevan_cafes.csv`
- **Selenium Scraper:** `yerevan_cafes_selenium.csv`

فایل CSV قابل باز شدن در Excel/Google Sheets است.

---

## آمار نمونه

بعد از اجرای موفق، آماری شبیه به این نمایش داده می‌شود:

```
============================================================
📊 آمار نهایی:
   • تعداد کل کافه‌ها: 245
   • کافه‌های با شماره تلفن: 198
   • کافه‌های با وب‌سایت: 87
   • کافه‌های با رتبه‌بندی: 156
============================================================

✅ استخراج با موفقیت انجام شد!
📁 فایل‌های خروجی: yerevan_cafes.json, yerevan_cafes.csv
```

---

## عیب‌یابی

### خطای "API Key نامعتبر"
- مطمئن شوید API Key را صحیح وارد کرده‌اید
- بررسی کنید که Key منقضی نشده باشد
- از Platform Manager وضعیت subscription خود را چک کنید

### خطای "ChromeDriver not found" (Selenium)
- مطمئن شوید ChromeDriver نصب شده است
- نسخه ChromeDriver باید با نسخه Chrome مطابقت داشته باشد
- ChromeDriver را در PATH سیستم قرار دهید

### خطای "Timeout"
- اتصال اینترنت خود را بررسی کنید
- ممکن است نیاز به افزایش timeout در کد باشد
- از VPN استفاده کنید اگر سایت در کشور شما فیلتر است

### داده‌های ناقص
- برخی کافه‌ها ممکن است همه اطلاعات را نداشته باشند
- این طبیعی است و به کیفیت داده‌های موجود در 2GIS بستگی دارد

### خطای Rate Limiting (API)
- تعداد درخواست‌ها در نسخه رایگان محدود است
- می‌توانید تاخیر بین درخواست‌ها را افزایش دهید
- یا subscription خریداری کنید

---

## تنظیمات پیشرفته

### تغییر شعاع جستجو (API Scraper)

در `2gis_api_scraper.py` خط 28:

```python
SEARCH_RADIUS = 20000  # به متر (20 کیلومتر)
```

### تغییر حداکثر تعداد کافه (Selenium)

در `2gis_selenium_scraper.py` خط 27:

```python
MAX_CAFES = 500  # حداکثر تعداد
```

### تغییر مختصات مرکز جستجو (API Scraper)

در `2gis_api_scraper.py` خطوط 25-28:

```python
YEREVAN_CENTER = {
    "lat": 40.1872,
    "lon": 44.5152
}
```

---

## پشتیبانی

اگر با مشکلی مواجه شدید یا سوالی دارید، لطفاً یک Issue باز کنید.

---

## مجوز

این اسکریپت‌ها فقط برای اهداف آموزشی و تحقیقاتی ارائه شده‌اند. لطفاً قوانین استفاده از 2GIS API و شرایط خدمات آن‌ها را رعایت کنید.

**توجه:** استفاده تجاری از این داده‌ها ممکن است نیاز به مجوز خاصی از 2GIS داشته باشد.
